#include<stdio.h>
int even();
int odd();
int main()
{
    int i,z,a,b;
    printf("enter the number:");
    for(i=0;i<=2;i++)
    {
        scanf("%d",&z);
    }
    printf("even numbers are:%d\n",even(a));
    printf("odd numbers are:%d\n",odd(b));
}
int even(int a)
{
    int s,i;
    for(i=0;i<=2;i++)
    {
    if(s%2==0)
    {
        return s;
    }
}}
int odd(int b)
{
    int m,i;
    for(i=0;i<=2;i++)
    {
    if(m%2!=0)
    {
        return m;
    }
}}
